# clawrise-cli

This is the npm root package README template.

During release, the final README and install commands are generated from the actual package name so the published scope, version, and command examples stay consistent.
