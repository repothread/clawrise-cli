# clawrise-cli

This file is the source README template for the published npm root package.

During release, `scripts/release/prepare-npm-packages.mjs` generates the final root-package README from this template so the published package name, install command, and AI install guide URL stay aligned with the current release metadata.
