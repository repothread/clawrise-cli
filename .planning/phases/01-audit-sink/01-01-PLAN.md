---
phase: 01-audit-sink
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/runtime/audit_sink.go
  - internal/runtime/governance.go
autonomous: true
requirements:
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "pluginAuditSink.Emit() 不再在每次调用后关闭插件进程"
    - "runtimeGovernance 在生命周期结束时统一关闭所有 pluginAuditSink 进程"
    - "连续调用两次 Emit 均成功写入审计事件，无错误日志"
  artifacts:
    - path: "internal/runtime/audit_sink.go"
      provides: "修复 pluginAuditSink.Emit() 移除 defer Close()，新增 closePluginAuditSinks 辅助函数"
      contains: "closePluginAuditSinks"
    - path: "internal/runtime/governance.go"
      provides: "runtimeGovernance 新增 closeSinks() 方法，writeAudit 后不再自动关闭"
      contains: "closeSinks"
  key_links:
    - from: "internal/runtime/governance.go"
      to: "internal/runtime/audit_sink.go"
      via: "governance.closeSinks() 调用 closePluginAuditSinks()"
      pattern: "closePluginAuditSinks"
---

<objective>
修复审计 sink 插件进程在首次 emit 后被错误关闭的 bug。

Purpose: pluginAuditSink.Emit() 中的 defer s.runtime.Close() 会在第一次审计事件写入后终止插件子进程，导致后续 emit 全部失败。需要将 Close 职责从单次 Emit 提升到 runtimeGovernance 生命周期级别。

Output: 修复后的 audit_sink.go 和 governance.go，插件进程在整个 CLI 调用期间保持可用。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/runtime/audit_sink.go
@internal/runtime/governance.go
@internal/runtime/executor.go
@internal/plugin/audit_sink.go
@internal/plugin/process.go
</context>

<interfaces>
<!-- executor 中通过 auditEnvelope -> governance.writeAudit -> governance.emitAuditSinks 调用链 -->

From internal/runtime/audit_sink.go:
```go
type pluginAuditSink struct {
    runtime pluginruntime.AuditSinkRuntime
}

func (s *pluginAuditSink) Emit(ctx context.Context, record auditRecord) error {
    // BUG: defer s.runtime.Close() 在首次 emit 后关闭进程
    defer func() {
        _ = s.runtime.Close()
    }()
    return s.runtime.Emit(ctx, pluginruntime.AuditEmitParams{...})
}
```

From internal/runtime/governance.go:
```go
type runtimeGovernance struct {
    paths        runtimePaths
    store        governanceStore
    sinks        []auditSink      // 审计 sink 列表
    sinkWarnings []string
    retry        retryPolicy
    now          func() time.Time
}

func (g *runtimeGovernance) emitAuditSinks(record auditRecord) []string {
    // 遍历所有 sink 调用 Emit
    for _, sink := range g.sinks {
        sink.Emit(context.Background(), record)
    }
}
```

From internal/plugin/audit_sink.go:
```go
type AuditSinkRuntime interface {
    Name() string
    ID() string
    Priority() int
    Handshake(ctx context.Context) (HandshakeResult, error)
    Emit(ctx context.Context, params AuditEmitParams) error
    Close() error
}
```

From internal/plugin/process.go line 288:
```go
// Close 终止插件子进程并释放资源
func (r *ProcessRuntime) Close() error {
    // Kill 子进程、关闭 stdin、清理资源
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 移除 pluginAuditSink.Emit() 中的 defer Close() 并添加 lifecycle Close 支持</name>
  <files>internal/runtime/audit_sink.go, internal/runtime/governance.go</files>
  <read_first>
    - internal/runtime/audit_sink.go (完整文件，确认当前 Emit 实现和 pluginAuditSink 结构)
    - internal/runtime/governance.go (完整文件，确认 runtimeGovernance 结构和 writeAudit/emitAuditSinks 方法)
    - internal/runtime/executor.go (确认 auditEnvelope 调用链和 governance 的使用方式)
    - internal/plugin/audit_sink.go (确认 AuditSinkRuntime 接口和 ProcessAuditSink.Close)
    - internal/plugin/process.go (第 288 行附近，确认 Close 终止子进程的行为)
  </read_first>
  <action>
修复 pluginAuditSink 在每次 Emit 后错误关闭插件进程的 bug。

**audit_sink.go 修改（实现 BUG-01 和 BUG-02）:**

1. 在 `pluginAuditSink.Emit()` 方法（第 125-138 行）中：
   - 删除整个 `defer func() { _ = s.runtime.Close() }()` 块（第 129-132 行）
   - 保留 `s.runtime.Emit(ctx, ...)` 调用不变
   - 在方法前添加注释说明为什么不在此处关闭：`// 注意：不在 Emit 中关闭 runtime，因为插件进程需要在整个 CLI 调用生命周期内保持可用。关闭由 runtimeGovernance.closeSinks() 统一处理。`

2. 在 audit_sink.go 文件末尾新增辅助函数：
```go
// closePluginAuditSinks 关闭所有 pluginAuditSink 持有的插件进程。
// 仅在 runtimeGovernance 生命周期结束时调用（BUG-01/BUG-02 修复）。
func closePluginAuditSinks(sinks []auditSink) {
    for _, sink := range sinks {
        if pluginSink, ok := sink.(*pluginAuditSink); ok && pluginSink != nil {
            _ = pluginSink.runtime.Close()
        }
    }
}
```

**governance.go 修改:**

1. 在 `runtimeGovernance` 结构体上新增 `closeSinks()` 方法（放在 `emitAuditSinks` 方法之后）：
```go
// closeSinks 关闭所有需要显式清理的审计 sink（如插件进程）。
// 在 runtimeGovernance 生命周期结束时调用。
func (g *runtimeGovernance) closeSinks() {
    if g == nil {
        return
    }
    closePluginAuditSinks(g.sinks)
}
```

2. 在 executor.go 的 `Execute` 方法中，找到 `return e.auditEnvelope(governance, envelope, input), nil` 这一行（最后一个 return），在其前面添加 `defer governance.closeSinks()`：
   - 具体位置：executor.go 第 235 行 `return e.auditEnvelope(governance, envelope, input), nil` 之前
   - 添加 `defer governance.closeSinks()` 确保 Execute 返回时清理所有插件进程
   - 但要注意 Execute 中有多个提前返回路径，需要在 `newRuntimeGovernance` 创建后立即 defer
   - 精确位置：executor.go 第 65 行 `governance = newRuntimeGovernance(e.store.Path(), cfg, e.now)` 之后，添加 `defer governance.closeSinks()`
   - 同样在第 57 行初始 governance 创建后也要添加 defer（因为第一个 governance 可能被第二个替代，第一个没有 sinks 不影响）

实际操作：在 executor.go 第 65 行之后（第二个 newRuntimeGovernance 调用后）添加一行：
```go
defer governance.closeSinks()
```
这是因为第 57 行创建的临时 governance 会在第 65 行被覆盖，而第 65 行创建的才是真正使用的实例。如果在第 65 行之后 defer，会正确关闭最终使用的 governance 的 sinks。
  </action>
  <verify>
    <automated>cd /Users/liyang/thread/clawrise-cli && go test ./internal/runtime/... -v -count=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `audit_sink.go` 中 `pluginAuditSink.Emit()` 方法不再包含 `s.runtime.Close()` 调用（grep 验证：`grep -n "s.runtime.Close()" internal/runtime/audit_sink.go` 返回空或仅出现在 closePluginAuditSinks 中）
    - `audit_sink.go` 包含新函数 `closePluginAuditSinks`（grep 验证：`grep -n "func closePluginAuditSinks" internal/runtime/audit_sink.go` 有输出）
    - `governance.go` 包含新方法 `closeSinks`（grep 验证：`grep -n "func.*closeSinks" internal/runtime/governance.go` 有输出）
    - `executor.go` 包含 `defer governance.closeSinks()`（grep 验证：`grep -n "closeSinks" internal/runtime/executor.go` 有输出）
    - `go test ./internal/runtime/...` 全部通过
    - `go build ./...` 编译成功
  </acceptance_criteria>
  <done>pluginAuditSink.Emit() 不再关闭插件进程；runtimeGovernance.closeSinks() 在 Execute 返回时统一清理；所有现有测试通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加多次 emit 和 close 生命周期的测试用例</name>
  <files>internal/runtime/executor_test.go</files>
  <read_first>
    - internal/runtime/executor_test.go (第 2010-2108 行，TestExecutorEmitsAuditSinkPlugin 测试，了解现有的审计 sink 插件测试模式)
    - internal/runtime/audit_sink.go (确认修复后的 Emit 和 closePluginAuditSinks 实现)
    - internal/runtime/governance.go (确认 closeSinks 方法)
    - internal/runtime/executor.go (确认 defer governance.closeSinks() 位置)
  </read_first>
  <behavior>
    - 测试 1: 单次 Execute 中连续两次写入审计事件（通过 batch 场景或构造多次 writeAudit），插件进程收到两次 emit RPC 调用，marker 文件包含两行审计记录
    - 测试 2: 验证 closeSinks 后再次 Emit 不会 panic（即关闭后 sink 不可用但不崩溃）
    - 测试 3: 验证非插件 sink（stdout、webhook）不受 closeSinks 影响
  </behavior>
  <action>
在 executor_test.go 文件末尾（`newTestRegistry` 函数之前）添加新的测试函数。

**测试 1: TestExecutorPluginAuditSinkSupportsMultipleEmitsInOneCall**
验证 BUG-02 修复：同一 CLI 调用中多次审计事件写入。

复用 TestExecutorEmitsAuditSinkPlugin 的模式：
1. 创建临时 pluginRoot 目录和 audit-demo 插件（与现有测试相同模式）
2. marker 文件记录所有收到的 emit 请求
3. 配置 demo 操作，执行第一次 Execute
4. 执行第二次 Execute（不同 idempotency key）
5. 验证 marker 文件包含 2 行 `"method":"clawrise.audit.emit"` 的记录
6. 验证两次执行均成功（envelope.OK == true）

```go
func TestExecutorPluginAuditSinkSupportsMultipleEmitsInOneCall(t *testing.T) {
    pluginRoot := t.TempDir()
    t.Setenv("CLAWRISE_PLUGIN_PATHS", pluginRoot)
    t.Setenv("HOME", t.TempDir())

    markerPath := filepath.Join(t.TempDir(), "audit-sink-multi.jsonl")
    pluginDir := filepath.Join(pluginRoot, "audit-multi", "0.1.0")
    if err := os.MkdirAll(pluginDir, 0o755); err != nil {
        t.Fatalf("failed to create audit sink plugin dir: %v", err)
    }
    script := fmt.Sprintf(`#!/bin/sh
marker=%q
while IFS= read -r line; do
  case "$line" in
    *'"method":"clawrise.audit.emit"'*)
      printf '%%s\n' "$line" >> "$marker"
      printf '{"jsonrpc":"2.0","id":"1","result":{}}'"\n"
      ;;
  esac
done
`, markerPath)
    if err := os.WriteFile(filepath.Join(pluginDir, "audit-multi.sh"), []byte(script), 0o755); err != nil {
        t.Fatalf("failed to write audit sink plugin executable: %v", err)
    }
    if err := os.WriteFile(filepath.Join(pluginDir, "plugin.json"), []byte(`{
  "schema_version": 2,
  "name": "audit-multi",
  "version": "0.1.0",
  "protocol_version": 1,
  "capabilities": [
    {
      "type": "audit_sink",
      "id": "capture",
      "priority": 50
    }
  ],
  "entry": {
    "type": "binary",
    "command": ["./audit-multi.sh"]
  }
}`), 0o644); err != nil {
        t.Fatalf("failed to write audit sink plugin manifest: %v", err)
    }

    store := newTestStore(t, &config.Config{
        Defaults: config.Defaults{
            Platform: "demo",
            Account:  "demo_operator",
        },
        Accounts: map[string]config.Account{
            "demo_operator": {
                Platform: "demo",
                Subject:  "integration",
                Auth: config.AccountAuth{
                    Method: "demo.token",
                },
            },
        },
    })

    registry := adapter.NewRegistry()
    registry.Register(adapter.Definition{
        Operation:       "demo.page.update",
        Platform:        "demo",
        Mutating:        true,
        DefaultTimeout:  time.Second,
        AllowedSubjects: []string{"integration"},
        Spec: adapter.OperationSpec{
            Summary: "测试审计 sink 多次写入。",
            Idempotency: adapter.IdempotencySpec{
                Required: true,
            },
        },
        Handler: func(ctx context.Context, call adapter.Call) (map[string]any, *apperr.AppError) {
            return map[string]any{"ok": true}, nil
        },
    })

    executor := NewExecutor(store, registry)

    // 第一次执行
    firstEnvelope, err := executor.ExecuteContext(context.Background(), ExecuteOptions{
        OperationInput: "demo.page.update",
        InputJSON:      `{"id":"page_1"}`,
        IdempotencyKey: "idem_multi_first",
    })
    if err != nil {
        t.Fatalf("first ExecuteContext returned error: %v", err)
    }
    if !firstEnvelope.OK {
        t.Fatalf("expected first execution to succeed, got: %+v", firstEnvelope.Error)
    }

    // 第二次执行 - 使用不同的 idempotency key
    secondEnvelope, err := executor.ExecuteContext(context.Background(), ExecuteOptions{
        OperationInput: "demo.page.update",
        InputJSON:      `{"id":"page_2"}`,
        IdempotencyKey: "idem_multi_second",
    })
    if err != nil {
        t.Fatalf("second ExecuteContext returned error: %v", err)
    }
    if !secondEnvelope.OK {
        t.Fatalf("expected second execution to succeed, got: %+v", secondEnvelope.Error)
    }

    // 验证插件收到两次 emit
    data, err := os.ReadFile(markerPath)
    if err != nil {
        t.Fatalf("failed to read audit sink marker: %v", err)
    }
    lines := strings.Count(string(data), `"method":"clawrise.audit.emit"`)
    if lines != 2 {
        t.Fatalf("expected 2 audit emit calls to plugin, got %d; marker content:\n%s", lines, string(data))
    }
}
```

**测试 2: TestPluginAuditSinkCloseLifecycle**
验证 closeSinks 的行为正确性。

在 executor_test.go 中添加（放在上面的测试后面）：

```go
func TestPluginAuditSinkCloseLifecycle(t *testing.T) {
    // 验证：closePluginAuditSinks 仅关闭 plugin 类型的 sink，不影响 stdout/webhook sink
    t.Run("closes_only_plugin_sinks", func(t *testing.T) {
        var stdoutBuf bytes.Buffer
        sinks := []auditSink{
            &stdoutAuditSink{writer: &stdoutBuf},
            &pluginAuditSink{runtime: nil}, // nil runtime 不应 panic
        }
        // 调用 closePluginAuditSinks 不应 panic
        closePluginAuditSinks(sinks)

        // stdout sink 仍然可用
        record := auditRecord{Operation: "test.op", OK: true}
        if err := sinks[0].Emit(context.Background(), record); err != nil {
            t.Fatalf("stdout sink should still work after closePluginAuditSinks: %v", err)
        }
        if !strings.Contains(stdoutBuf.String(), "test.op") {
            t.Fatalf("expected stdout output to contain operation, got: %s", stdoutBuf.String())
        }
    })
}
```

注意：`closePluginAuditSinks` 函数在 `audit_sink.go` 中是包内可见的（小写开头），所以测试文件在同 package 下可以直接调用。

两个测试都必须在 `go test ./internal/runtime/...` 下通过。
  </action>
  <verify>
    <automated>cd /Users/liyang/thread/clawrise-cli && go test ./internal/runtime/... -run "TestExecutorPluginAuditSinkSupportsMultipleEmitsInOneCall|TestPluginAuditSinkCloseLifecycle" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - executor_test.go 包含 `TestExecutorPluginAuditSinkSupportsMultipleEmitsInOneCall` 函数（grep 验证有输出）
    - executor_test.go 包含 `TestPluginAuditSinkCloseLifecycle` 函数（grep 验证有输出）
    - `go test ./internal/runtime/... -run TestExecutorPluginAuditSinkSupportsMultipleEmitsInOneCall` 通过
    - `go test ./internal/runtime/... -run TestPluginAuditSinkCloseLifecycle` 通过
    - `go test ./internal/runtime/...` 全部通过（回归测试）
  </acceptance_criteria>
  <done>
    - 测试证明同一插件进程可以接收多次 audit emit 而不被关闭
    - 测试证明 closePluginAuditSinks 仅关闭 plugin 类型 sink，不影响 stdout/webhook
    - 所有现有测试无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtimeGovernance -> pluginAuditSink | runtime 层管理插件进程的生命周期，需确保不泄露进程 |
| pluginAuditSink -> 外部插件进程 | 通过 stdin/stdout JSON-RPC 与子进程通信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | D (Denial of Service) | pluginAuditSink | accept | 插件进程保持存活时间从"首次 emit"延长到"CLI 退出"，增加了资源占用窗口。但 CLI 是短生命周期工具，单次调用结束后即退出，进程自然释放。风险可接受。 |
| T-01-02 | I (Information Disclosure) | closePluginAuditSinks | mitigate | Close 错误被静默忽略（`_ = pluginSink.runtime.Close()`），避免在清理阶段泄露错误详情到不安全的输出通道。 |
| T-01-03 | T (Tampering) | pluginAuditSink.Emit | accept | 修复不改变 emit 的数据流，仅调整调用时机。原有输入验证和 redaction 逻辑不变。 |
</threat_model>

<verification>
1. `go test ./internal/runtime/... -v -count=1` 全部通过
2. `go build ./...` 编译成功
3. grep 验证 Emit 中无 s.runtime.Close()，closePluginAuditSinks 存在，closeSinks 存在
4. grep 验证 executor.go 包含 defer governance.closeSinks()
</verification>

<success_criteria>
- pluginAuditSink.Emit() 不再调用 s.runtime.Close()
- runtimeGovernance 新增 closeSinks() 方法
- executor.go Execute 方法中 defer governance.closeSinks()
- 新增测试 TestExecutorPluginAuditSinkSupportsMultipleEmitsInOneCall 通过
- 新增测试 TestPluginAuditSinkCloseLifecycle 通过
- 所有现有测试无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/01-audit-sink/01-01-SUMMARY.md`
</output>
