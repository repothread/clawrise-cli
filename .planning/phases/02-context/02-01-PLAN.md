---
phase: 02-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/root.go
autonomous: true
requirements: [CTX-01, CTX-02]

must_haves:
  truths:
    - "root.go 的 Run() 函数创建 signal.NotifyContext，支持 SIGINT 取消所有操作"
    - "executor.ExecuteContext 接收调用方 context 而非 context.Background()"
    - "doctor 命令的 InspectDiscoveryWithOptions 和 InspectAuth 接收调用方 context"
    - "runtime_options.go 的 nil 防护代码保持不变，上游 ctx 正确传入时不会触发"
  artifacts:
    - path: "internal/cli/root.go"
      provides: "signal.NotifyContext 根 context 创建和传递"
      contains: "signal.NotifyContext"
  key_links:
    - from: "internal/cli/root.go"
      to: "internal/runtime/executor.go"
      via: "executor.ExecuteContext(ctx, ...)"
      pattern: "ExecuteContext\\(ctx,"
---

<objective>
在 root.go 的 Run() 入口创建 signal.NotifyContext，将可取消的 context 传递给所有操作执行路径（runOperation、runDoctor 等），使 CLI 支持 Ctrl+C 中断。

Purpose: 使 CLI 操作执行路径尊重操作系统 SIGINT 信号，用户按 Ctrl+C 可取消进行中的操作。同时验证 runtime_options.go 的 nil 防护代码在正确 ctx 传入时不会被触发（CTX-02 实际不需要修改代码）。
Output: 修改后的 internal/cli/root.go，所有 context.Background() 替换为调用方 context。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-context/02-CONTEXT.md
@.planning/phases/02-context/02-RESEARCH.md

<interfaces>
<!-- executor 的 ExecuteContext 签名，root.go 需要传递 ctx 给它 -->
<!-- 来源：internal/runtime/executor.go -->
```go
func (e *Executor) ExecuteContext(ctx context.Context, opts ExecuteOptions) (Envelope, error)
```

<!-- plugin runtime 的 Manager 方法签名 -->
<!-- 来源：internal/plugin/runtime.go -->
```go
func NewDiscoveredManagerWithOptions(ctx context.Context, options DiscoveryOptions) (*Manager, error)
func InspectDiscoveryWithOptions(ctx context.Context, options DiscoveryOptions) (*DiscoveryInspection, error)
func (m *Manager) InspectAuth(ctx context.Context, platform string, params AuthInspectParams) (*AuthInspection, error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 root.go 创建 signal.NotifyContext 并传递给所有操作执行路径</name>
  <files>internal/cli/root.go</files>
  <read_first>
    - internal/cli/root.go (完整文件，当前所有 context.Background() 调用)
  </read_first>
  <behavior>
    - Run() 函数开头创建 signal.NotifyContext(context.Background(), os.Interrupt)，defer cancel()
    - newDefaultPluginManager 接受 ctx 参数并传递给 NewDiscoveredManagerWithOptions
    - runOperation 接受 ctx 参数并传递给 executor.ExecuteContext
    - runDoctor 接受 ctx 参数并传递给 InspectDiscoveryWithOptions 和 InspectAuth
    - resolvePluginManager 接受 ctx 参数并透传给 newDefaultPluginManager
    - 所有 switch 分支中调用这些函数时传入 ctx
  </behavior>
  <action>
    1. 在 root.go 顶部 import 块添加 `"os"` 和 `"os/signal"`（如果还没有的话）

    2. 修改 `Run()` 函数，在 `store, err := config.ResolveStore()` 之前插入：
       ```go
       ctx, cancel := signal.NotifyContext(context.Background(), os.Interrupt)
       defer cancel()
       ```

    3. 修改 `resolvePluginManager` 签名，添加 ctx 参数：
       ```go
       func resolvePluginManager(ctx context.Context, deps Dependencies, store *config.Store) (*pluginruntime.Manager, error) {
       ```
       将 ctx 传递给 newDefaultPluginManager。

    4. 修改 `newDefaultPluginManager` 签名：
       ```go
       func newDefaultPluginManager(ctx context.Context, store *config.Store) (*pluginruntime.Manager, error) {
       ```
       将 line 157 的 `context.Background()` 替换为传入的 `ctx`。

    5. 修改 `runOperation` 签名：
       ```go
       func runOperation(ctx context.Context, args []string, stdout io.Writer, stderr io.Writer, executor *runtime.Executor) error {
       ```
       将 line 280 的 `context.Background()` 替换为传入的 `ctx`。

    6. 修改 `runDoctor` 签名：
       ```go
       func runDoctor(ctx context.Context, store *config.Store, stdout io.Writer, manager *pluginruntime.Manager) error {
       ```
       将 line 460 的 `context.Background()` 替换为 `ctx`。
       将 line 555 的 `context.Background()` 替换为 `ctx`。

    7. 在 `Run()` 函数的 switch 分支中：
       - 所有调用 `resolvePluginManager(deps, store)` 改为 `resolvePluginManager(ctx, deps, store)`
       - `runOperation(args, deps.Stdout, deps.Stderr, executor)` 改为 `runOperation(ctx, args, deps.Stdout, deps.Stderr, executor)`
       - `runDoctor(store, deps.Stdout, manager)` 改为 `runDoctor(ctx, store, deps.Stdout, manager)`
       - 其他 case 分支（platform, account, subject, version, auth, secret, config, batch, spec, docs, completion, plugin）不需要 ctx 参数，但它们的 `resolvePluginManager` 调用需要传入 ctx

    8. 不修改的函数（per D-05 排除范围）：
       - runAuth、runConfig、runBatch 等非操作执行路径不需要 ctx 参数
       - 但这些函数内部调用 resolvePluginManager 时也需要传入 ctx

    注意：context.Context 作为第一个参数传递（per D-02 和 Go 惯例）。
    注意：os/signal import 中 signal 已通过 pluginruntime 别名间接可用，但 os/signal 需要显式导入。
  </action>
  <verify>
    <automated>cd /Users/liyang/thread/clawrise-cli && go build ./internal/cli/... && go test ./internal/cli/... -count=1</automated>
  </verify>
  <done>
    - root.go 中不存在 `context.Background()` 调用（grep 验证：`grep -n 'context.Background()' internal/cli/root.go` 返回空）
    - root.go 包含 `signal.NotifyContext` 调用
    - `go build ./internal/cli/...` 编译通过
    - `go test ./internal/cli/... -count=1` 全部通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证 runtime_options.go 的 nil 防护在 ctx 传入后不被触发，运行全量相关测试</name>
  <files>internal/adapter/runtime_options.go (只读验证)</files>
  <read_first>
    - internal/adapter/runtime_options.go (验证 nil 防护代码保持不变)
    - internal/runtime/executor.go (确认 executor.go 已经将 ctx 传递给 WithRuntimeOptions 等函数)
  </read_first>
  <action>
    1. 确认 runtime_options.go 的 4 处 `if ctx == nil { ctx = context.Background() }` 防护代码未被动（不应修改这些代码，它们是正确的防御性编程）

    2. 验证 executor.go 中 WithRuntimeOptions、WithRequestID、WithProviderDebugCapture 的调用已经使用传入的 ctx 参数（非 context.Background()）—— 根据代码审计，executor.go:200-206 已经使用 ctx 参数调用这些函数

    3. 运行完整的相关测试套件确认无回归：
       ```bash
       go test ./internal/cli/... ./internal/adapter/... ./internal/runtime/... -count=1
       ```

    本任务不修改任何文件，仅验证 CTX-02 的要求已被 Task 1 的 root.go 修改覆盖（上游正确传入 ctx 后，runtime_options.go 的 nil fallback 不会触发）。
  </action>
  <verify>
    <automated>cd /Users/liyang/thread/clawrise-cli && go test ./internal/cli/... ./internal/adapter/... ./internal/runtime/... -count=1</automated>
  </verify>
  <done>
    - runtime_options.go 包含 4 处 nil 防护代码（grep 验证：`grep -c 'ctx == nil' internal/adapter/runtime_options.go` 输出 4 或更多）
    - executor.go 中 WithRuntimeOptions/WithRequestID/WithProviderDebugCapture 使用 ctx 参数（grep 验证：`grep -n 'adapter.WithRuntimeOptions(ctx' internal/runtime/executor.go` 有匹配）
    - 全量相关测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户终端 -> CLI | SIGINT 信号从用户终端传入 CLI 进程 |
| CLI -> Executor | context 取消信号从 CLI 入口传播到执行引擎 |
| Executor -> HTTP client | context 取消信号传播到 HTTP 请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | root.go signal.NotifyContext | mitigate | signal.NotifyContext 自动清理 signal handler（defer cancel()），不会泄漏 goroutine |
| T-02-02 | Tampering | executor.ExecuteContext | accept | context 取消导致操作中断是预期行为，idempotency 机制支持安全重试 |
| T-02-03 | Denial of Service | runtime_options.go nil fallback | accept | nil fallback 是防御性编程，正常流程中不会被触发 |
</threat_model>

<verification>
1. `grep -n 'context.Background()' internal/cli/root.go` 返回空 -- 所有 context.Background() 已替换
2. `grep -n 'signal.NotifyContext' internal/cli/root.go` 有匹配 -- signal context 已创建
3. `go test ./internal/cli/... ./internal/adapter/... ./internal/runtime/... -count=1` 全部通过
4. `go build ./...` 编译通过
</verification>

<success_criteria>
- root.go 中 4 处 context.Background() 全部替换为调用方 ctx（per CTX-01 lines 157, 280, 460, 555）
- Run() 函数在入口处创建 signal.NotifyContext（per D-02）
- runtime_options.go 的 nil 防护代码未被修改（per CTX-02，上游 ctx 正确传入即满足需求）
- 所有相关测试通过，无回归
</success_criteria>

<output>
After completion, create `.planning/phases/02-context/02-01-SUMMARY.md`
</output>
