---
phase: 02-context
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/plugin/install.go
autonomous: true
requirements: [CTX-04]

must_haves:
  truths:
    - "install.go 的 HTTP 下载请求使用 http.NewRequestWithContext 传递 context"
    - "inspectRuntimeCapabilities 在 InfoInstalledWithOptions 中接收调用方 context"
    - "resolveRegistrySource 中的 runtime.Resolve 接收调用方 context"
    - "下载中断时 context 取消可终止进行中的 HTTP 请求"
  artifacts:
    - path: "internal/plugin/install.go"
      provides: "插件安装流程的 context 传播"
      contains: "NewRequestWithContext"
  key_links:
    - from: "internal/plugin/install.go"
      to: "net/http"
      via: "http.NewRequestWithContext(ctx, ...)"
      pattern: "NewRequestWithContext"
    - from: "internal/plugin/install.go"
      to: "internal/plugin/capability_inspection.go"
      via: "inspectRuntimeCapabilities(ctx, manifest)"
      pattern: "inspectRuntimeCapabilities\\(ctx"
---

<objective>
修改 install.go 的插件安装流程，将 context 从公开 API 方法沿调用链传递到 HTTP 下载和 runtime.Resolve 调用，使插件下载和验证操作可被 context 取消。

Purpose: 使插件安装的下载/验证操作支持 context 取消，下载中断时能正确终止 HTTP 请求和清理资源。
Output: 修改后的 internal/plugin/install.go，所有 HTTP 请求和外部调用传递调用方 context。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-context/02-CONTEXT.md
@.planning/phases/02-context/02-RESEARCH.md

<interfaces>
<!-- install.go 中需要添加 ctx 参数的函数签名链 -->

<!-- 当前公开 API（不修改签名，内部使用 context.Background() 作为 fallback） -->
```go
func Install(source string) (InstallResult, error)
func InstallWithOptions(source string, options InstallOptions) (InstallResult, error)
func InfoInstalled(name, version string) (PluginInfo, error)
func InfoInstalledWithOptions(name, version string, options DiscoveryOptions) (PluginInfo, error)
```

<!-- inspectRuntimeCapabilities 已接受 ctx 参数 -->
<!-- 来源：internal/plugin/capability_inspection.go:19 -->
```go
func inspectRuntimeCapabilities(ctx context.Context, manifest Manifest) runtimeCapabilityInspection
```

<!-- RegistrySourceRuntime.Resolve 已接受 ctx 参数 -->
```go
func (r *RegistrySourceRuntime) Resolve(ctx context.Context, params RegistrySourceResolveParams) (RegistrySourceResolveResult, error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 给 install.go 内部函数链添加 ctx 参数，替换 context.Background() 和 http.NewRequest</name>
  <files>internal/plugin/install.go</files>
  <read_first>
    - internal/plugin/install.go (完整文件，重点关注函数签名链和 HTTP 调用点)
    - internal/plugin/capability_inspection.go (确认 inspectRuntimeCapabilities 已接受 ctx)
  </read_first>
  <behavior>
    - InstallWithOptions、InfoInstalledWithOptions 等公开 API 签名不变，内部使用 context.Background() 作为 fallback
    - resolveInstallCandidate 接受 ctx 并传递给 materializeSource
    - materializeSource 接受 ctx 并传递给 downloadRemoteSource/resolveNPMSource/resolveRegistrySource
    - downloadRemoteSource 接受 ctx，http.NewRequest 改为 http.NewRequestWithContext
    - resolveNPMSource 接受 ctx，http.NewRequest 改为 http.NewRequestWithContext，并传递给 downloadRemoteSource
    - resolveRegistrySource 接受 ctx，传递给 runtime.Resolve 和 downloadRemoteSource
    - InfoInstalledWithOptions 中 inspectRuntimeCapabilities 调用改为传入 ctx（不再硬编码 context.Background()）
  </behavior>
  <action>
    1. **修改 downloadRemoteSource** —— 添加 ctx 参数，替换 HTTP 调用：
       ```go
       func downloadRemoteSource(ctx context.Context, source, tempDir string, options InstallOptions) (remoteDownloadResult, error) {
       ```
       将 line 871:
       ```go
       request, err := http.NewRequest(http.MethodGet, source, nil)
       ```
       改为:
       ```go
       request, err := http.NewRequestWithContext(ctx, http.MethodGet, source, nil)
       ```

    2. **修改 resolveNPMSource** —— 添加 ctx 参数，替换 HTTP 调用：
       ```go
       func resolveNPMSource(ctx context.Context, reference installSourceReference, tempDir string, options InstallOptions) (string, string, string, error) {
       ```
       将 line 919:
       ```go
       request, err := http.NewRequest(http.MethodGet, packageURL, nil)
       ```
       改为:
       ```go
       request, err := http.NewRequestWithContext(ctx, http.MethodGet, packageURL, nil)
       ```
       将 line 968 的 `downloadRemoteSource(versionMetadata.Dist.Tarball, tempDir, options)` 改为 `downloadRemoteSource(ctx, versionMetadata.Dist.Tarball, tempDir, options)`

    3. **修改 resolveRegistrySource** —— 添加 ctx 参数，替换 context.Background()：
       ```go
       func resolveRegistrySource(ctx context.Context, reference installSourceReference, tempDir string, options InstallOptions) (string, string, string, error) {
       ```
       将 line 1004:
       ```go
       resolveResult, resolveErr := runtime.Resolve(context.Background(), RegistrySourceResolveParams{
       ```
       改为:
       ```go
       resolveResult, resolveErr := runtime.Resolve(ctx, RegistrySourceResolveParams{
       ```
       将 line 1032 的 `downloadRemoteSource(resolveResult.ArtifactURL, tempDir, options)` 改为 `downloadRemoteSource(ctx, resolveResult.ArtifactURL, tempDir, options)`

    4. **修改 materializeSource** —— 添加 ctx 参数并透传：
       ```go
       func materializeSource(ctx context.Context, reference installSourceReference, tempDir string, options InstallOptions) (string, string, string, error) {
       ```
       - HTTP/HTTPS case: `downloadRemoteSource(source, tempDir, options)` 改为 `downloadRemoteSource(ctx, source, tempDir, options)`
       - NPM case: `resolveNPMSource(reference, tempDir, options)` 改为 `resolveNPMSource(ctx, reference, tempDir, options)`
       - Registry case: `resolveRegistrySource(reference, tempDir, options)` 改为 `resolveRegistrySource(ctx, reference, tempDir, options)`

    5. **修改 resolveInstallCandidate** —— 添加 ctx 参数：
       ```go
       func resolveInstallCandidate(ctx context.Context, source string, options InstallOptions) (installCandidate, func(), error) {
       ```
       将 line 444 的 `materializeSource(reference, tempDir, options)` 改为 `materializeSource(ctx, reference, tempDir, options)`

    6. **修改 InstallWithOptions** —— 内部创建 context.Background() 作为 fallback：
       ```go
       func InstallWithOptions(source string, options InstallOptions) (InstallResult, error) {
           ctx := context.Background()
           // ... 保持现有逻辑 ...
           candidate, cleanup, err := resolveInstallCandidate(ctx, source, options)
       ```
       注意：InstallWithOptions 是公开 API，签名不变，内部使用 context.Background()。

    7. **修改 InfoInstalledWithOptions** —— 替换 inspectRuntimeCapabilities 的 context.Background()：
       在函数内部添加 `ctx := context.Background()` 或直接将 line 373 的：
       ```go
       capabilityInspection := inspectRuntimeCapabilities(context.Background(), manifest)
       ```
       改为：
       ```go
       ctx := context.Background()
       capabilityInspection := inspectRuntimeCapabilities(ctx, manifest)
       ```
       或更简洁地，保持使用 `context.Background()` 传入 inspectRuntimeCapabilities——因为这个调用的 ctx 已经在函数内部创建。关键是消除硬编码的 `context.Background()` 传给需要 ctx 的方法。由于 InfoInstalledWithOptions 签名不变，这里保持 `context.Background()` 是可以接受的。

    8. **修改 checkUpgradeCandidate** 中对 resolveInstallCandidate 的调用，传入 ctx：
       ```go
       candidate, cleanup, err := resolveInstallCandidate(ctx, source, options)
       ```
       需要在 checkUpgradeCandidate 内部创建 ctx 或接受 ctx 参数。由于 checkUpgradeCandidate 是内部函数，添加 ctx 参数：
       ```go
       func checkUpgradeCandidate(ctx context.Context, name, version string, options InstallOptions) (UpgradeResult, *installCandidate, error) {
       ```
       然后在 UpgradeInstalled 中传入 ctx。UpgradeInstalled 是公开 API，签名不变，内部创建 `ctx := context.Background()`。

    9. **修改 UpgradeInstalled**：
       ```go
       func UpgradeInstalled(name, version string, options InstallOptions) (UpgradeResult, error) {
           ctx := context.Background()
           result, candidate, err := checkUpgradeCandidate(ctx, name, version, options)
       ```

    10. **修改 UpgradeAllInstalled**：不需要修改签名，内部 UpgradeInstalled 调用不需要传 ctx。

    每一步修改后确保编译通过。
  </action>
  <verify>
    <automated>cd /Users/liyang/thread/clawrise-cli && go build ./internal/plugin/... && go test ./internal/plugin/... -count=1</automated>
  </verify>
  <done>
    - `grep -n 'http.NewRequest(' internal/plugin/install.go` 返回空（所有 HTTP 请求已改用 NewRequestWithContext）
    - `grep -n 'NewRequestWithContext' internal/plugin/install.go` 有匹配
    - `grep -n 'context.Background()' internal/plugin/install.go` 仅出现在公开 API 函数内部（InstallWithOptions、UpgradeInstalled 等）的 fallback ctx 创建处
    - downloadRemoteSource、resolveNPMSource、resolveRegistrySource、materializeSource、resolveInstallCandidate 函数签名均包含 ctx context.Context 参数
    - `go test ./internal/plugin/... -count=1` 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> plugin install | context 取消信号传播到插件下载流程 |
| install.go -> HTTP server | context 传播到远程 HTTP 请求 |
| install.go -> registry plugin | context 传播到 registry source 解析 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of Service | downloadRemoteSource | accept | context 取消后 Go net/http 自动关闭连接，defer response.Body.Close() 已存在 |
| T-02-07 | Tampering | resolveRegistrySource | accept | 下载中断后不完整文件在 tempDir 中，cleanup 函数会清理 tempDir |
</threat_model>

<verification>
1. `grep -n 'http.NewRequest(' internal/plugin/install.go` 返回空
2. `grep -n 'NewRequestWithContext' internal/plugin/install.go` 有匹配
3. `grep -n 'func downloadRemoteSource' internal/plugin/install.go` 签名包含 ctx context.Context
4. `go test ./internal/plugin/... -count=1` 全部通过
</verification>

<success_criteria>
- install.go 中 2 处 http.NewRequest 改为 http.NewRequestWithContext（lines 871, 919）
- inspectRuntimeCapabilities 调用传递 ctx（line 373 区域）
- runtime.Resolve 调用传递 ctx（line 1004 区域）
- 所有内部函数（downloadRemoteSource、resolveNPMSource、resolveRegistrySource、materializeSource、resolveInstallCandidate）接受 ctx 参数
- 公开 API（InstallWithOptions、InfoInstalledWithOptions、UpgradeInstalled）签名不变
- 所有 plugin 测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-context/02-03-SUMMARY.md`
</output>
